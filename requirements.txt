# Core numerical / training stack
numpy>=1.24
scipy>=1.10
tqdm>=4.66
PyYAML>=6.0

# PyTorch: install the CUDA build that matches your system.
# Example for CUDA 12.1:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.3

# Tokenization and model/data utilities
tokenizers>=0.15
transformers>=4.40
datasets>=2.18
huggingface_hub>=0.22
safetensors>=0.4
sentence-transformers>=2.6

# Data processing / inspection
pandas>=2.0
pyarrow>=15.0
scikit-learn>=1.3

# API / teacher generation utilities
requests>=2.31
openai>=1.30

# Plotting / experiment inspection
matplotlib>=3.7

# Optional but useful for notebooks / debugging
ipython>=8.0
jupyter>=1.0
