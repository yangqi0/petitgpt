[pytest]
# Only collect the real unit-test suite under tests/. The distill/ tree contains
# standalone API/GPU smoke scripts named test_*.py that are not pytest tests and
# would try to hit external services on collection.
testpaths = tests
addopts = -q
